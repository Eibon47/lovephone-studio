# LovePhone OS 发布核对记录

更新时间：2026-08-12

## 已完成

- 角色新增、编辑、复制、删除、切换和角色数据隔离。
- 每个角色独立的聊天会话、记忆、日记、晚安记录和 AI 服务商选择。
- 真实 AI 接入、流式回复、停止、重新生成、删除、清空和中文错误提示。
- API Key 通过本机桥接服务保存，使用 Windows DPAPI 加密，不进入浏览器配置或导出文件。
- IndexedDB 数据保存、旧数据迁移、自动备份、手动备份、恢复、容量和保存失败提示。
- 可安装 PWA、独立手机页面和包含本地桥接服务的 Windows 桌面包。
- 音乐、小组件、图片上传、天气与主要配置开关的实际业务联动。
- 本地桥接服务来源校验、会话 Cookie、静态资源安全头和导入图片注入防护。
- 自动化测试、桌面/手机尺寸界面验收和依赖漏洞审计。
- 安全自定义系统、整机与 App 实时预览、声明式小组件及离线 ZIP 主题包。
- 自定义 CSS 作用域隔离、外链拦截、ZIP 路径/体积/类型检查和 SHA-256 防篡改。
- 自定义 App 包导入预检、沙箱运行、按 App ID 隔离存储、权限收回、更新保留数据、卸载清理与离线成品导出。
- GitHub CI：每次推送和 Pull Request 自动执行测试与网页构建；Dependabot 每周检查依赖更新。

## 当前验证结果

- `npm test`：145 项通过，0 项失败（2026-08-12 本地验证）。
- `npm run web:build`：通过（2026-08-12 本地验证）。
- 依赖审计：2026-08-12 使用官方 registry 执行 `npm audit --registry=https://registry.npmjs.org`，结果为 0 个已知漏洞。已移除未参与运行的旧网易云 CLI 开发依赖，并通过 overrides 将 `music-metadata`、`file-type` 与 `js-yaml` 提升到已修复版本。
- 腾讯云在线音乐：`/music-gateway` 已切回原有 `wangyiyun66-gateway`，健康检查和真实歌曲搜索通过；云函数自身依赖审计为 0 个已知漏洞。
- 开源构建不包含维护者的 AI 或音乐网关；私有部署默认值只通过被 Git 忽略的本地文件注入。
- CloudBase AI 和音乐网关已启用前端来源白名单：内测站点健康检查与音乐搜索通过，未授权网站请求返回 403。
- AI 桥接：陌生来源返回 403，无会话返回 401，密钥存储报告为 `windows-dpapi`。
- 音乐：本地文件使用浏览器原生播放器；在线音乐仅在用户配置兼容 API 后启用。
- 桌面编辑器与独立手机页：无横向溢出，控制台无错误。
- 自定义侧边面板：1440×1000 与 414×835 视口通过，底部操作按钮可见，正文独立滚动。

## 公开发布前必须处理

- 音乐 App 已改为浏览器原生播放；公开版不再依赖 MPV、网易云 CLI 或本机音乐桥接。
- 正式安装包启用 ASAR 封装；Windows 代码签名仍待后续配置。
- 购买或配置 Windows 代码签名证书，为安装包和主程序签名，减少系统安全警告。
- 确认网易云音乐接口、账号数据、封面、歌词和歌曲播放的公开及商业使用授权。
- 准备隐私政策、用户协议、内容安全规则、客服与数据删除说明。
- 使用真实但受控的各家模型账号做上线前兼容测试，并设置费用限额和异常监控。
- 在 GitHub 仓库设置中启用 Security Advisories、分支保护和 Actions；确认 CI 首次绿色通过。
- 公开自定义 App 示例时只使用自有素材，并在安装页面与隐私说明中持续明确第三方代码、数据权限、联网域名和 AI 额度风险。

在以上外部事项完成前，安装包仅作为本机测试版，不对外公开分发。
