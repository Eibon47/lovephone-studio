# LovePhone OS 发布核对记录

更新时间：2026-07-24

## 已完成

- 角色新增、编辑、复制、删除、切换和角色数据隔离。
- 每个角色独立的聊天会话、记忆、日记、晚安记录和 AI 服务商选择。
- 真实 AI 接入、流式回复、停止、重新生成、删除、清空和中文错误提示。
- API Key 通过本机桥接服务保存，使用 Windows DPAPI 加密，不进入浏览器配置或导出文件。
- IndexedDB 数据保存、旧数据迁移、自动备份、手动备份、恢复、容量和保存失败提示。
- 可安装 PWA、独立手机页面和包含本地桥接服务的 Windows 桌面包。
- 音乐、小组件、图片上传、天气与主要配置开关的实际业务联动。
- 本地桥接服务来源校验、会话 Cookie、静态资源安全头和导入图片注入防护。
- 自动化测试、桌面/手机尺寸界面验收和依赖漏洞审计。

## 当前验证结果

- `npm test`：51 项通过，0 项失败。
- `npm audit`：0 个已知漏洞。
- AI 桥接：陌生来源返回 403，无会话返回 401，密钥存储报告为 `windows-dpapi`。
- 音乐桥接：陌生来源返回 403，无会话返回 401。
- 桌面编辑器与独立手机页：无横向溢出，控制台无错误。

## 公开发布前必须处理

- 购买或配置 Windows 代码签名证书，为安装包和主程序签名，减少系统安全警告。
- 核验随包 MPV 二进制的准确构建方式，并履行对应 GPL/LGPL 和依赖许可证义务。
- 确认网易云音乐接口、账号数据、封面、歌词和歌曲播放的公开及商业使用授权。
- 准备隐私政策、用户协议、内容安全规则、客服与数据删除说明。
- 使用真实但受控的各家模型账号做上线前兼容测试，并设置费用限额和异常监控。

在以上外部事项完成前，安装包仅作为本机测试版，不对外公开分发。
